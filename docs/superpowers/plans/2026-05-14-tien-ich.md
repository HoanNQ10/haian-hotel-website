# Trang Tiện Ích Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a dedicated `/tien-ich` page listing 3 hotel services (Spa, Thuê Xe, Đưa Đón) and rename "Tiện Nghi" → "Tiện Ích" site-wide.

**Architecture:** New Astro page at `src/pages/tien-ich.astro` following the exact same pattern as `src/pages/phong-nghi.astro` — alternating 2-column grid cards with gradient placeholder left/right. Header and Footer updated to point "Tiện Ích" nav links to `/tien-ich`.

**Tech Stack:** Astro 4, Tailwind CSS 3

---

## File Map

- **Create:** `src/pages/tien-ich.astro` — dedicated services page
- **Modify:** `src/components/Header.astro` — update "Tiện Nghi" desktop (line 44) + mobile (line 99): text rename + href `/#co-so` → `/tien-ich`
- **Modify:** `src/components/Footer.astro` — update "Tiện Nghi" in "Khám Phá" column (line 58): text rename + href `/#co-so` → `/tien-ich`

---

### Task 1: Create `/tien-ich` services page

**Files:**
- Create: `src/pages/tien-ich.astro`

- [ ] **Step 1: Create the file**

Create `src/pages/tien-ich.astro` with this exact content:

```astro
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const services = [
  {
    name: "Spa",
    description: "Thư giãn và phục hồi năng lượng với các liệu pháp spa chuyên nghiệp ngay tại khách sạn.",
    gradient: "bg-gradient-to-br from-accent-soft to-border"
  },
  {
    name: "Thuê Xe",
    description: "Thuê xe máy và ô tô linh hoạt theo ngày — khám phá Đà Lạt và Bảo Lộc theo cách của bạn.",
    gradient: "bg-gradient-to-br from-primary/10 to-primary/20"
  },
  {
    name: "Đưa Đón",
    description: "Dịch vụ đưa đón sân bay và các điểm du lịch — đặt lịch trước, đúng giờ, an toàn.",
    gradient: "bg-gradient-to-br from-accent/20 to-accent-soft"
  }
];
---

<Layout title="Tiện Ích — Hải An Villa Hotel">
  <Header />

  <!-- Page Hero -->
  <section class="bg-surface border-b border-border">
    <div class="max-w-container mx-auto px-4 md:px-8 py-10">
      <a href="/" class="inline-flex items-center gap-1 text-sm text-text-muted hover:text-primary transition-colors duration-200 mb-4">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
        Trang Chủ
      </a>
      <h1 class="text-3xl md:text-4xl font-heading font-bold text-primary mb-3">Tiện Ích</h1>
      <div class="w-12 h-0.5 bg-accent mb-4"></div>
      <p class="text-text-muted max-w-xl">Các dịch vụ và tiện ích tại Hải An Villa Hotel.</p>
    </div>
  </section>

  <!-- Service List -->
  <section class="bg-background">
    <div class="max-w-container mx-auto px-4 md:px-8 py-14">
      <div class="space-y-12">
        {services.map((service, index) => (
          <>
            <div class={`flex flex-col md:grid md:grid-cols-2 gap-8 md:gap-12 items-center${index % 2 !== 0 ? ' [&>*:first-child]:md:order-last' : ''}`}>
              <div class={`w-full aspect-video rounded-xl ${service.gradient}`}></div>
              <div>
                <h2 class="text-2xl font-heading font-bold text-primary mb-3">{service.name}</h2>
                <p class="text-text-muted leading-relaxed mb-6">{service.description}</p>
                <a
                  href="/lien-he"
                  class="inline-flex items-center justify-center gap-2 bg-accent text-white px-6 py-3 rounded-lg text-sm font-semibold hover:bg-accent-dark transition-all duration-200 hover:shadow-md hover:-translate-y-0.5 active:translate-y-0"
                >
                  Liên Hệ →
                </a>
              </div>
            </div>
            {index < services.length - 1 && <hr class="border-border" />}
          </>
        ))}
      </div>
    </div>
  </section>

  <Footer />
</Layout>
```

- [ ] **Step 2: Verify build passes**

Run: `npm run build`

Expected: build completes with no errors, `dist/tien-ich/index.html` is generated alongside `dist/index.html`, `dist/phong-nghi/index.html`, `dist/lien-he/index.html`.

- [ ] **Step 3: Commit**

```bash
git add src/pages/tien-ich.astro
git commit -m "feat: add /tien-ich dedicated services page with Spa, Thue Xe, Dua Don"
```

---

### Task 2: Rename "Tiện Nghi" → "Tiện Ích" in Header and Footer

**Files:**
- Modify: `src/components/Header.astro`
- Modify: `src/components/Footer.astro`

- [ ] **Step 1: Update Header desktop nav (line 44)**

In `src/components/Header.astro`, change:
```html
<a href="/#co-so" class="text-text-muted hover:text-primary text-sm font-medium transition-colors duration-200 relative group">
        Tiện Nghi
```
To:
```html
<a href="/tien-ich" class="text-text-muted hover:text-primary text-sm font-medium transition-colors duration-200 relative group">
        Tiện Ích
```

- [ ] **Step 2: Update Header mobile nav (line 99)**

In `src/components/Header.astro`, change:
```html
<a href="/#co-so" class="block text-text-muted hover:text-primary hover:bg-accent-soft text-sm py-3 px-3 rounded-lg transition-all duration-200 font-medium">Tiện Nghi</a>
```
To:
```html
<a href="/tien-ich" class="block text-text-muted hover:text-primary hover:bg-accent-soft text-sm py-3 px-3 rounded-lg transition-all duration-200 font-medium">Tiện Ích</a>
```

- [ ] **Step 3: Update Footer "Khám Phá" column (line 58)**

In `src/components/Footer.astro`, change:
```html
<li><a href="/#co-so" class="hover:text-white transition-colors duration-200">Tiện Nghi</a></li>
```
To:
```html
<li><a href="/tien-ich" class="hover:text-white transition-colors duration-200">Tiện Ích</a></li>
```

- [ ] **Step 4: Verify build passes**

Run: `npm run build`

Expected: build completes with no errors, 4 pages generated (`/`, `/phong-nghi`, `/lien-he`, `/tien-ich`).

- [ ] **Step 5: Commit**

```bash
git add src/components/Header.astro src/components/Footer.astro
git commit -m "feat: rename Tien Nghi to Tien Ich and update nav links to /tien-ich"
```
